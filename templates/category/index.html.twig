{% extends 'base.html.twig' %}

{% block content %}
    <h1>Liste des Catégories</h1>

    <ul>
        {% for category in categories %}
            <li>{{ category.name }}</li>
        {% endfor %}
    </ul>
{% endblock %}
